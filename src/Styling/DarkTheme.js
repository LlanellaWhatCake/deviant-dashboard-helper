import { createMuiTheme } from '@material-ui/core';

export default theme = createMuiTheme({
    palette: {
      primary: {
        main: "#00c787",
        dark: "#01a570"
      },
      secondary: {
        main: "#505459",
        dark: "#353a40"
      },
      backgroundMain: {
        main: "#1e2125"
      }
    }
  });